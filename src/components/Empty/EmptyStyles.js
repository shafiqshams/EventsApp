import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  emptyWrapper: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  imageStyle: {
    width: 400,
    height: 300,
    resizeMode: 'contain',
  },
});
